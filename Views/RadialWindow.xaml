<Window x:Class="Polaris.Views.RadialWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="{x:Null}"
        ShowInTaskbar="False"
        Topmost="True"
        ShowActivated="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        AllowDrop="True"
        DragOver="OnDragOverPanel"
        Drop="OnDropPanel"
        Title="Polaris">
    <Grid x:Name="RootGrid">
        <Canvas x:Name="PanelCanvas"/>
    </Grid>
</Window>
